<%= form_with(model: itemtest) do |form| %>
  <% if itemtest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(itemtest.errors.count, "error") %> prohibited this itemtest from being saved:</h2>

      <ul>
        <% itemtest.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
<%= form.hidden_field :user_id %> 
  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
  <div class="field">
    <%#= form.label :Upload an image %>
  <%= form.file_field :image %>
  </div>
<% end %>
